#--------------------------------------
# This is a manifest for a Cloud Foundry app 
# Change the `host` property to a unique hostname 
# on the domain you are deploying to.
#--------------------------------------

applications:
- name:    Hello Cloud-2
  host:    cf-node-hellocloud2-dcn167
  memory:  128M
  build: nodejs_buildpack-cached-v1.2.0.zip
